# Mosquitto Configuration for AquaSmart Development
# Listen on all network interfaces (WiFi + localhost)

listener 1883 0.0.0.0
allow_anonymous true

# Logging (verbose mode)
log_dest stdout
log_type all
log_timestamp true
